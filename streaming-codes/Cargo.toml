[package]
name = "streaming-codes"
version = "0.1.0"
edition = "2021"
description = "Streaming erasure codes (Badr/Martinian delay-optimal two-layer construction)"
license = "MIT OR Apache-2.0"

[dependencies]
gf256 = { path = "../gf256" }
bytes = "1"
